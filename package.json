{
  "name": "x-backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "hono": "^4.9.2",
    "nodemailer": "^7.0.5"
  },
  "scripts": {
    "start": "start": "pm2 start index.js --name x && pm2 logs x",
     "stop": "pm2 stop all"
  }
}
